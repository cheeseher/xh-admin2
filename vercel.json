{
  "version": 2,
  "buildCommand": "cd xinghai-admin && npm install && npm install terser --save-dev && npm run build",
  "outputDirectory": "xinghai-admin/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
} 